# FitBridge MVP — Airtable persistence config
# Copy to .env.local and fill in real values, then restart `npm run dev`.
# If these are unset, the app falls back to localStorage so the demo still works.

VITE_AIRTABLE_API_KEY=patXXXXXXXXXXXXXX.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
VITE_AIRTABLE_BASE_ID=appXXXXXXXXXXXXXX

# Optional — override default table names
VITE_AIRTABLE_TABLE_MEALS=Meals
VITE_AIRTABLE_TABLE_METRICS=BodyMetrics
VITE_AIRTABLE_TABLE_WORKOUT_LOGS=WorkoutLogs
VITE_AIRTABLE_TABLE_SCANS=MealScans
VITE_AIRTABLE_TABLE_USERS=Users
VITE_AIRTABLE_TABLE_TRAINER_NOTES=TrainerNotes
VITE_AIRTABLE_TABLE_LINKS=TrainerClientLink
VITE_AIRTABLE_TABLE_MESSAGES=Messages

# Nutrition lookup — primary provider is USDA FoodData Central (free, broad
# coverage of common + branded + international foods via the FNDDS dataset).
# Get a key at https://fdc.nal.usda.gov/api-key-signup.html
VITE_USDA_API_KEY=

# Optional secondary provider — Nutritionix dev tier
# (https://developer.nutritionix.com/). Used as a fallback when USDA returns
# zero results — primarily helpful for branded restaurant items like
# "In-N-Out", "Chipotle bowl", etc. If both are unset, search falls back to
# the small built-in food catalog and most queries will return no results.
VITE_NUTRITIONIX_APP_ID=
VITE_NUTRITIONIX_APP_KEY=

# ---- Google Maps + Places (Find-a-Gym screen) -----------------------------
# Powers the interactive map + nearby-gym search on /connect/gym.
#
# Setup:
#   1. https://console.cloud.google.com → New project.
#   2. APIs & Services → Library → enable BOTH:
#        - Maps JavaScript API
#        - Places API
#   3. Enable billing on the project. Google requires a billing account
#      even for the free tier. Free monthly credit ($200/mo) is generous
#      for a prototype — typical usage during user testing comes in well
#      under that.
#   4. Credentials → Create credentials → API key.
#        - Application restrictions: HTTP referrers
#        - Allowed referrers:
#            http://localhost:*/*
#            https://fitbridge.netlify.app/*
#        - API restrictions: Maps JavaScript API + Places API.
#   5. Paste the key below and restart `npm run dev`.
#   6. For Netlify: Site settings → Environment variables → add
#        VITE_GOOGLE_MAPS_API_KEY=AIza…
#      and trigger a redeploy.
#
# WITHOUT this key the gym screen falls back to a CSS placeholder map
# + the seed gym list. The connection flow stays usable.
#
# Note: this key IS bundled into the frontend JS (unavoidable for a
# browser-side Maps SDK). The HTTP-referrer restriction in step 4 is
# what protects you from quota theft.
VITE_GOOGLE_MAPS_API_KEY=
